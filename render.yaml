services:
  - type: web
    name: email-spam-filter-api
    env: python
    plan: free
    buildCommand: pip install -r requirements-deploy.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT api:app
    autoDeploy: true
